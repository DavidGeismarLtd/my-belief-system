<div class="min-h-screen bg-gradient-to-br from-blue-50 via-white to-purple-50 flex items-center justify-center px-4 py-8">
  <div class="max-w-2xl w-full">
    <!-- Header -->
    <div class="text-center mb-8 sm:mb-10">
      <div class="inline-block mb-4 px-4 py-2 bg-purple-100 text-purple-700 rounded-full text-sm font-semibold">
        Step 1 of 2
      </div>
      <h1 class="text-3xl sm:text-4xl font-bold text-gray-900 mb-3">
        Before We Begin
      </h1>
      <p class="text-base sm:text-lg text-gray-600 max-w-xl mx-auto">
        Help us personalize your experience with a few quick questions
      </p>
    </div>

    <!-- Profile Form -->
    <div class="bg-white/80 backdrop-blur-sm rounded-2xl shadow-xl border border-gray-100 p-8 sm:p-10">
      <%= form_with url: onboarding_save_profile_path, method: :post, class: "space-y-6" do |f| %>

        <!-- Country Selection -->
        <div>
          <label class="block text-sm font-bold text-gray-900 mb-2">
            Which country do you live in? <span class="text-red-500">*</span>
          </label>
          <p class="text-sm text-gray-600 mb-3">This helps us show you relevant political actors and questions</p>
          <select name="country" required class="w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:border-blue-500 focus:ring-2 focus:ring-blue-200 focus:outline-none transition-all duration-200 bg-white hover:border-gray-300">
            <option value="">Select your country</option>
            <% OnboardingController::COUNTRIES.each do |country| %>
              <option value="<%= country %>"><%= country %></option>
            <% end %>
          </select>
        </div>

        <!-- Age -->
        <div>
          <label class="block text-sm font-bold text-gray-900 mb-2">
            What's your age? <span class="text-gray-400 font-normal">(Optional)</span>
          </label>
          <input type="number" name="age" min="18" max="120"
                 class="w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:border-blue-500 focus:ring-2 focus:ring-blue-200 focus:outline-none transition-all duration-200 hover:border-gray-300"
                 placeholder="e.g., 35">
        </div>

        <!-- Gender -->
        <div>
          <label class="block text-sm font-bold text-gray-900 mb-2">
            How do you identify? <span class="text-gray-400 font-normal">(Optional)</span>
          </label>
          <select name="gender" class="w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:border-blue-500 focus:ring-2 focus:ring-blue-200 focus:outline-none transition-all duration-200 bg-white hover:border-gray-300">
            <option value="">Prefer not to say</option>
            <option value="male">Male</option>
            <option value="female">Female</option>
            <option value="non_binary">Non-binary</option>
            <option value="other">Other</option>
          </select>
        </div>

        <!-- Political Engagement -->
        <div>
          <label class="block text-sm font-bold text-gray-900 mb-2">
            How politically engaged are you? <span class="text-gray-400 font-normal">(Optional)</span>
          </label>
          <select name="political_engagement" class="w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:border-blue-500 focus:ring-2 focus:ring-blue-200 focus:outline-none transition-all duration-200 bg-white hover:border-gray-300">
            <option value="">Select one</option>
            <option value="very_engaged">Very engaged - I follow politics daily</option>
            <option value="moderately_engaged">Moderately engaged - I keep up with major news</option>
            <option value="somewhat_engaged">Somewhat engaged - I check in occasionally</option>
            <option value="not_engaged">Not very engaged - I rarely follow politics</option>
          </select>
        </div>

        <!-- Privacy Note -->
        <div class="bg-gradient-to-r from-blue-50 to-purple-50 border-l-4 border-blue-500 p-5 rounded-lg">
          <div class="flex items-start gap-3">
            <div class="flex-shrink-0 text-2xl">🔒</div>
            <div>
              <p class="text-sm font-semibold text-gray-900 mb-1">Your privacy matters</p>
              <p class="text-sm text-gray-700">
                This information helps us personalize your experience. We never share your data with third parties.
              </p>
            </div>
          </div>
        </div>

        <!-- Submit Button -->
        <div class="pt-4">
          <%= f.submit "Continue to Questions", class: "w-full bg-gradient-to-r from-blue-600 to-purple-600 hover:from-blue-700 hover:to-purple-700 text-white px-6 py-4 rounded-xl text-lg font-bold shadow-lg hover:shadow-xl transition-all duration-200 cursor-pointer transform hover:scale-[1.02]" %>
        </div>
      <% end %>
    </div>

    <!-- Back Link -->
    <div class="text-center mt-6">
      <%= link_to "← Back to Home", root_path, class: "text-gray-600 hover:text-gray-900 font-medium transition-colors" %>
    </div>
  </div>
</div>
