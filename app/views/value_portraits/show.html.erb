<div class="min-h-screen bg-gradient-to-br from-blue-50 via-white to-purple-50 py-8 px-4">
  <div class="max-w-6xl mx-auto">
    <!-- Header -->
    <div class="mb-8">
      <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between gap-4">
        <div>
          <div class="inline-block mb-2 px-3 py-1 bg-blue-100 text-blue-700 rounded-full text-xs font-semibold">
            📊 Your Value Portrait
          </div>
          <h1 class="text-3xl sm:text-4xl font-bold text-gray-900 mb-2">
            My <span class="bg-gradient-to-r from-blue-600 to-purple-600 bg-clip-text text-transparent">Value Portrait</span>
          </h1>
          <p class="text-gray-600 font-medium">Created <%= time_ago_in_words(@portrait[:created_at]) %> ago</p>
        </div>
        <div class="flex gap-3">
          <%= link_to "← Dashboard", dashboard_path, class: "inline-flex items-center gap-2 text-blue-600 hover:text-blue-700 font-semibold transition-colors" %>
          <%= link_to "Refine Values", onboarding_path, class: "bg-gradient-to-r from-blue-600 to-purple-600 hover:from-blue-700 hover:to-purple-700 text-white px-6 py-3 rounded-xl font-bold shadow-lg hover:shadow-xl transition-all duration-200 transform hover:scale-105" %>
        </div>
      </div>
    </div>

    <!-- Interactive Radar Chart -->
    <div class="bg-white/80 backdrop-blur-sm rounded-2xl shadow-xl border border-gray-100 p-8 sm:p-10 mb-8">
      <h2 class="text-2xl font-bold text-gray-900 mb-6 text-center">Your Political Values at a Glance</h2>
      <div class="relative w-full max-w-3xl mx-auto"
           data-controller="radar-chart"
           data-radar-chart-dimensions-value="<%= @portrait[:dimensions].map { |d| d[:name] }.to_json %>"
           data-radar-chart-user-values-value="<%= @portrait[:dimensions].map { |d| d[:position] }.to_json %>"
           data-radar-chart-show-comparison-value="false">

        <!-- SVG Container -->
        <div class="bg-gradient-to-br from-gray-50 to-white rounded-xl p-8 sm:p-12 relative">
          <svg data-radar-chart-target="svg" class="w-full h-auto" style="min-height: 400px;"></svg>

          <!-- Tooltip -->
          <div data-radar-chart-target="tooltip" class="hidden absolute z-10"></div>
        </div>

        <!-- Legend -->
        <div class="flex justify-center gap-6 mt-6 text-sm">
          <div class="flex items-center gap-2">
            <div class="w-4 h-4 bg-blue-500 rounded-full shadow-sm"></div>
            <span class="text-gray-700 font-medium">Your Values</span>
          </div>
        </div>

        <p class="text-center text-sm text-gray-500 mt-4">
          Hover over the data points to see detailed values for each dimension
        </p>
      </div>
    </div>

    <!-- Detailed Dimensions -->
    <div class="space-y-5">
      <h2 class="text-2xl sm:text-3xl font-bold text-gray-900 mb-8 text-center">Detailed Breakdown</h2>

      <% @portrait[:dimensions].each do |dimension| %>
        <div class="group bg-white/80 backdrop-blur-sm rounded-2xl shadow-lg hover:shadow-xl border border-gray-100 hover:border-blue-200 p-6 sm:p-8 transition-all duration-300">
          <h3 class="text-xl font-bold text-gray-900 mb-5 flex items-center gap-3">
            <span class="text-2xl">📊</span>
            <%= dimension[:name] %>
          </h3>

          <!-- Position Indicator -->
          <div class="mb-6">
            <div class="flex justify-between text-sm font-semibold text-gray-700 mb-3">
              <span class="text-left flex-1"><%= dimension[:left_pole] %></span>
              <span class="text-center px-4 text-gray-400">←</span>
              <span class="text-right flex-1"><%= dimension[:right_pole] %></span>
            </div>
            <div class="relative h-4 bg-gradient-to-r from-blue-100 via-gray-100 to-red-100 rounded-full shadow-inner">
              <% position_percent = ((dimension[:position] + 100) / 2.0).round %>
              <div class="absolute top-1/2 -translate-y-1/2 w-6 h-6 bg-gradient-to-br from-blue-500 to-blue-600 rounded-full shadow-lg ring-4 ring-white transition-transform duration-200 group-hover:scale-110"
                   style="left: calc(<%= position_percent %>% - 12px)"></div>
            </div>
            <div class="text-center mt-2">
              <span class="inline-block px-3 py-1 bg-blue-100 text-blue-700 rounded-full text-xs font-bold">
                Position: <%= dimension[:position] %>
              </span>
            </div>
          </div>

          <!-- Stats Grid -->
          <div class="grid sm:grid-cols-3 gap-4 mb-5">
            <div class="bg-gradient-to-br from-purple-50 to-purple-100 rounded-xl p-4 border border-purple-200 text-center">
              <div class="text-xs font-bold text-purple-900 mb-2">Lean</div>
              <div class="text-lg font-bold text-purple-700">
                <% if dimension[:position] < -20 %>
                  <%= dimension[:left_pole].split(' ').first %>
                <% elsif dimension[:position] > 20 %>
                  <%= dimension[:right_pole].split(' ').first %>
                <% else %>
                  Balanced
                <% end %>
              </div>
            </div>
            <div class="bg-gradient-to-br from-blue-50 to-blue-100 rounded-xl p-4 border border-blue-200 text-center">
              <div class="text-xs font-bold text-blue-900 mb-2">Intensity</div>
              <div class="text-lg font-bold text-blue-700">
                <% if dimension[:intensity] > 75 %>Strong
                <% elsif dimension[:intensity] > 50 %>Moderate
                <% else %>Weak<% end %>
                <span class="text-sm">(<%= (dimension[:intensity] / 100.0).round(2) %>)</span>
              </div>
            </div>
            <div class="bg-gradient-to-br from-green-50 to-green-100 rounded-xl p-4 border border-green-200 text-center">
              <div class="text-xs font-bold text-green-900 mb-2">Confidence</div>
              <div class="text-lg font-bold text-green-700">
                <% if dimension[:confidence] > 80 %>High
                <% elsif dimension[:confidence] > 60 %>Medium
                <% else %>Low<% end %>
                <span class="text-sm">(<%= (dimension[:confidence] / 100.0).round(2) %>)</span>
              </div>
            </div>
          </div>

          <!-- Explanation -->
          <div class="bg-gradient-to-r from-blue-50 to-purple-50 border-l-4 border-blue-500 p-5 rounded-xl">
            <div class="flex items-start gap-3">
              <span class="text-xl flex-shrink-0">💡</span>
              <div>
                <p class="text-sm font-semibold text-gray-900 mb-1">Why we think this:</p>
                <p class="text-sm text-gray-700 leading-relaxed">
                  <%= dimension[:explanation] %>
                </p>
              </div>
            </div>
          </div>
        </div>
      <% end %>
    </div>

    <!-- Actions -->
    <div class="mt-10 text-center">
      <%= link_to "See How Politicians Align →", dashboard_path, class: "inline-flex items-center gap-2 bg-gradient-to-r from-blue-600 to-purple-600 hover:from-blue-700 hover:to-purple-700 text-white px-8 py-4 rounded-xl font-bold shadow-lg hover:shadow-xl transition-all duration-200 transform hover:scale-105" %>
    </div>
  </div>
</div>
